# Database Configuration
# Required: Make sure to include ?schema=website_manager for the website_manager schema
DATABASE_URL="postgresql://user:password@localhost:5432/db_mame?schema=shema-name"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Environment
NODE_ENV="development"

# Proxy Configuration (for accessing Google APIs)
# If you need to access Google APIs through a proxy, configure these variables
# Example: http://localhost:7890
# For Node.js, these environment variables work globally:
HTTP_PROXY="http://localhost:7890"
HTTPS_PROXY="http://localhost:7890"

# Alternative: Use our custom proxy configuration
# USE_PROXY="true"
# PROXY_URL="http://localhost:7890"
