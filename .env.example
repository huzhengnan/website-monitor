# ============================================
# Database Configuration
# ============================================
# Required: Make sure to include ?schema=website_manager for the website_manager schema
# Example: postgresql://postgres:password@localhost:5432/website_monitor?schema=website_manager
DATABASE_URL="postgresql://user:password@localhost:5432/database_name?schema=website_manager"

# ============================================
# Application Configuration
# ============================================
# API URL for frontend to connect to backend
NEXT_PUBLIC_API_URL="http://localhost:3030"

# Environment mode (development, production)
NODE_ENV="development"

# ============================================
# Google Cloud Configuration (GA & GSC)
# ============================================
# Service account key files are used for Google Analytics and Google Search Console
#
# Place one of these files in the project root directory:
# - google-cloud-acount.json (preferred - matches code filename)
# - google-cloud-account.json (alternative spelling - also supported)
#
# Download service account key:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new service account
# 3. Grant "Editor" role or "Viewer" role
# 4. Create a JSON key
# 5. Download and rename to google-cloud-acount.json
#
# Service account email will be in the format: xxx@xxx.iam.gserviceaccount.com
# Add this email as owner in Google Search Console and GA properties
#
# Note: Key files are automatically ignored by git (see .gitignore)

# ============================================
# Proxy Configuration (Optional)
# ============================================
# If you need to access Google APIs through a corporate proxy
# Leave empty/commented if no proxy is needed
#
# Example proxy URL: http://localhost:7890 or http://proxy.company.com:8080
# These environment variables work globally for Node.js:
HTTP_PROXY="http://localhost:7890"
HTTPS_PROXY="http://localhost:7890"

# Alternative: Use custom proxy configuration (not recommended)
# USE_PROXY="true"
# PROXY_URL="http://localhost:7890"
